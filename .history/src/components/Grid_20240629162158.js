import React from 'react'

export default function Grid({currentGuess, guesses, turn}) {
    return (
        <div>
            {guesses.map(g) = {
                return
            }}
        </div>
    )
}